ReadME for CST310P4.cpp

Made by: Wesley Baker

To execute CST310P4.cpp make sure the files are in the directory that you attempt use the following commands on.

Next open up the command prompt/terminal and do the following: 
Note: Verify that you see CST310P4.cpp when performing the command "ls" on that directory, if you see them this means you can proceed.

To compile CST310P4.cpp type: g++ CST310P4.cpp -o P4 -lglut -lGLU -lGL

Following this,

To execute CST310P4.cpp type: ./P4

